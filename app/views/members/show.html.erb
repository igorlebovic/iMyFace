<h3>Member: <%= "#{@member.first_name} #{@member.last_name}" %></h3>

<p><%= link_to "Select a Member", members_path %></p>

<% if @member.outta %>
  <p>Outta: 
    <% for username in @member.outta %>
      <%= link_to username, member_path(username) %>
    <% end %>
  </p>
<% end %>

<% if @member.into %>
  <p>Into: 
    <% for username in @member.into %>
      <%= link_to username, member_path(username) %>
    <% end %>
  </p>
<% end %>